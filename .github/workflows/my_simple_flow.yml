name: Shell Commands

on: [push]

jobs:
  RunShellCommand:
    name: Run Shell Command
    runs-on: ubuntu-latest

    steps:
      - name: Echo a string
        run: echo "Hello World!!!"
      - name: Multiline script
        run: |
          node -v
          npm -v
